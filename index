<!DOCTYPE html>
<html lang="zh-Hant">
<head>
  <meta charset="UTF-8">
  <title>簡易抽獎程式</title>
</head>
<body>
  <h2>🎁 抽獎小程式</h2>
  <p>請輸入參加者名字（用「,」逗號分隔）：</p>
  <input type="text" id="names" value="小明,小美,阿強,麗麗" size="50">
  <br><br>
  <button onclick="draw()">抽出得獎者</button>

  <h3 id="result"></h3>

  <script>
    function draw() {
      const input = document.getElementById("names").value;
      const list = input.split(",").map(name => name.trim()).filter(name => name !== "");
      if (list.length === 0) {
        document.getElementById("result").innerText = "⚠️ 請輸入至少一位參加者";
        return;
      }
      const winner = list[Math.floor(Math.random() * list.length)];
      document.getElementById("result").innerText = `🎉 恭喜得獎者是：${winner}`;
    }
  </script>
</body>
</html>
